---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: everpeace/curl-jq

inputs:
- name: tools 
- name: check-actuator
outputs:
- name: build 

run:
  path: tools/scripts/build_previous_version.sh 
