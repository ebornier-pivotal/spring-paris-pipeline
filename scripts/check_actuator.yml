---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: everpeace/curl-jq

inputs:
- name: tools 
outputs:
- name: check-actuator 

run:
  path: tools/scripts/check_actuator.sh 
